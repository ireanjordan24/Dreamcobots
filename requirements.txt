# BuddyAI / Dreamcobots dependencies
# Core dependencies (all standard-library friendly – no external installs required for basic operation)

# Testing
pytest>=7.0

# Optional: enables real ML models in ml_optimizer.py
# scikit-learn>=1.3
# numpy>=1.24

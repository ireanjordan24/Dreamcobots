name: PR Learning

on:
  workflow_dispatch:
  pull_request:
    types: [closed]

jobs:
  learn:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run PR Learning Bot
        run: |
          python3 bots/pr_learning_bot.py

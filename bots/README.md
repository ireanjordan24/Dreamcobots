# Bots

This directory contains various bot scripts and automation tools used in the project.

## Files

- **debug.py** — Debug utility module. Set the `DISTUTILS_DEBUG` environment variable to any non-empty value to enable debug mode across bots.
- **config.json** — Configuration file for bot settings and API keys.
